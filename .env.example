# =============================================================================
# Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!
#
# Usage: cp .env.example .env

# -----------------------------------------------------------------------------
# Email Notification Settings (for security_notifier.py)
# -----------------------------------------------------------------------------

# SMTP Server Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here
SMTP_FROM=your-email@gmail.com

# Notification Recipients (comma-separated)
NOTIFY_EMAILS=security-team@company.com,devops@company.com

# -----------------------------------------------------------------------------
# Common SMTP Providers
# -----------------------------------------------------------------------------

# Gmail (requires App Password: https://myaccount.google.com/apppasswords)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# Outlook/Hotmail
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# Yahoo Mail
# SMTP_HOST=smtp.mail.yahoo.com
# SMTP_PORT=587

# Office 365
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587

# SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key

# Mailgun
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587

# -----------------------------------------------------------------------------
# Anthropic API Keys (for cost tracking)
# -----------------------------------------------------------------------------

# General API Key
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Admin API Key (for usage reports)
# Get this from your Anthropic organization settings
ANTHROPIC_ADMIN_API_KEY=your_admin_api_key_here

# -----------------------------------------------------------------------------
# MCP Configuration Encryption
# -----------------------------------------------------------------------------

# MCP Configuration Encryption Key
# This key is used to encrypt and decrypt your MCP server configuration
# IMPORTANT: Keep this key secure and never commit it to version control
# Minimum length: 16 characters (recommended: 32+ characters)
MCP_ENCRYPTION_KEY=your-secure-encryption-key-here-min-16-chars

# How to generate a secure key:
# - Windows PowerShell: -join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | ForEach-Object {[char]$_})
# - Linux/Mac: openssl rand -base64 32
# - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# -----------------------------------------------------------------------------
# Python MCP Servers
# -----------------------------------------------------------------------------

PYTHONUNBUFFERED=1
