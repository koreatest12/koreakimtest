version: "3.8"
services:
  defenderbot:
    build: .
    image: defenderbot:latest
    container_name: defenderbot
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=default
    restart: unless-stopped
