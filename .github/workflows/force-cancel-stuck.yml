name: "🏦 Manila Bank ULTRA Edition v10 — FULL SYSTEM"

on:
  workflow_dispatch:

jobs:

  init:
    uses: {OWNER}/{REPO}/.github/workflows/init.yml@main

  schema:
    needs: init
    uses: {OWNER}/{REPO}/.github/workflows/schema.yml@main

  trade_engine:
    needs: schema
    uses: {OWNER}/{REPO}/.github/workflows/trade-engine.yml@main

  fraud:
    needs: trade_engine
    uses: {OWNER}/{REPO}/.github/workflows/fraud-ai.yml@main

  multi_db:
    needs: fraud
    uses: {OWNER}/{REPO}/.github/workflows/multi-db.yml@main

  docker_cluster:
    needs: multi_db
    uses: {OWNER}/{REPO}/.github/workflows/docker-cluster.yml@main

  migration:
    needs: docker_cluster
    uses: {OWNER}/{REPO}/.github/workflows/migration.yml@main

  snapshot:
    needs: migration
    uses: {OWNER}/{REPO}/.github/workflows/snapshot.yml@main

  report:
    needs: snapshot
    uses: {OWNER}/{REPO}/.github/workflows/report.yml@main

  finalize:
    needs: report
    uses: {OWNER}/{REPO}/.github/workflows/finalize.yml@main
