# Teradata connection settings
TD_HOST=teradata.example.com
TD_PORT=1025
TD_USER=dbc
TD_PASSWORD=dbc
# Default database and logon mechanism (if needed)
TD_DATABASE=dbc
TD_AUTH=TD2

# Execution behavior
TD_SQL_DIR=/workspace/sql/teradata
TD_DATA_DIR=/workspace/data/teradata
TD_TIMEOUT_SECONDS=600
